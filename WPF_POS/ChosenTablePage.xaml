<Page x:Class="WPF_POS.ChosenTablePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_POS"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="550"
      Title="ChosenTablePage">

    <Border BorderThickness="10">
        <Grid MaxHeight="600" MaxWidth="550">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="6*" />
            </Grid.RowDefinitions>
            <Button x:Name="PreviousButton" Grid.Row="0" Grid.Column="0" Click="PreviousButton_Click">WSTECZ</Button>
            <Label x:Name="tableInfoLabel" Grid.Row="1" Grid.Column="0"/>
            <TextBlock Name="dateTextBlock" Grid.Row="1" Grid.Column="1"/>
            <ListBox x:Name="OrdersFromTableListBox" Height="200" Grid.Column="0" Grid.Row="2"></ListBox>
        </Grid>
    </Border>
   
</Page>
